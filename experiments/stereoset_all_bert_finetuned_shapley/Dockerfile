FROM stereoset_shapley:latest
ENV FINETUNE=True
ENV INCLUDE_UNRELATED=True
ARG NUM_SAMPLES
ARG GPU_CARD
ENV NUM_SAMPLES=$NUM_SAMPLES
ENV CUDA_VISIBLE_DEVICES=$GPU_CARD
ENV NVIDIA_VISIBLE_DEVICES=$GPU_CARD